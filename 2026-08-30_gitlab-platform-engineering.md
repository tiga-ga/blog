---
title: 【Platform Engineering×チームトポロジー】GitLabに学ぶ「開発者認知負荷ゼロ」の監視基盤設計——Metrics CatalogからDashboard as Codeまで
tags: PlatformEngineering, TeamTopologies, SRE, GitLab, Observability
author: tiga-ga (https://qiita.com/tiga-ga)
---

# 【Platform Engineering×チームトポロジー】GitLabに学ぶ「開発者認知負荷ゼロ」の監視基盤設計——Metrics CatalogからDashboard as Codeまで

世界で最もオープンに開発組織の運用ノウハウを公開している、GitLabのプラットフォーム監視基盤（Runbooks / Metrics Catalog）をAIと深掘りしながら読みました。

本記事は、[【第1部】GitLabに学ぶ「チーム別エラーバジェット」運用の完全ガイド](file:///Users/taiga.ogura/git/study/blog/2026-08-29_gitlab-error-budgets.md) の続編（第2部・基盤実装編）です。

---

## はじめに：なぜ「プラットフォームエンジニアリング」が必要なのか？

第1部で解説した通り、GitLabでは開発チーム（Stage Group）ごとにエラーバジェットを独立させ、障害や遅延を自律的に改善する運用を実現しています。
しかし、開発者が「コードにタグを1行書くだけ」でこの高度な運用が成立している裏側には、**中央のプラットフォームチーム（Infrastructure / Reliability基盤チーム）による徹底した自動化とセルフサービス基盤** が存在します。

もしプラットフォーム基盤がないままチーム別監視を導入すると、以下の「認知負荷の爆発」によって組織が自滅します。

### プラットフォームなき監視の失敗（Bad） vs GitLabのセルフサービス基盤（Good）

| 比較項目 | Bad (各チームに監視を丸投げした失敗) | Good (GitLabのプラットフォーム基盤) |
| :--- | :--- | :--- |
| **コードの帰属** | 誰のコードか分からず、障害発生時に責任の押し付け合いが発生 | **Feature Category**: コントローラーやジョブにチームタグを1行宣言するだけで自動紐付け |
| **タグ付けの徹底** | 人間の注意深さに依存し、タグ付け忘れによるメトリクス漏れが多発 | **CI静的解析**: DangerやRuboCopがマージ前に未分類コードを自動検知してブロック |
| **監視の構築** | 各チームのエンジニアが自前でPromQLを書き、GrafanaのGUIを操作してダッシュボードを作成 | **Dashboard as Code**: Jsonnetにより、全チーム専用のダッシュボードが全自動生成される |
| **バジェット管理** | スプレッドシートや手動計算で毎月集計し、管理コストで疲弊する | **メトリクス集計パイプライン**: Prometheusの記録ルールで直近28日間の消費時間を全自動計算 |
| **超過時の通知** | SREが毎月レポートを手作業で作成し、開発チームへ注意・叱責する | **Triage Bot**: 20分を超過したチームへ自動でGitLab Issueを起票してPM/EMへメンション |
| **チームの認知負荷** | 監視ツールの保守やクエリ調整に忙殺され、本来のプロダクト開発が停止 | **認知負荷ゼロ**: 開発チームは「自分たちのビジネスロジック」に100%集中できる |

---

## 読み進めていく中で気になったこと集

GitLabのプラットフォーム基盤（Runbooks / Metrics Catalog リポジトリ）のアーキテクチャを読み進める中で、以下の疑問が浮かび上がりました。

| 問い | 読み進める中で抱いた疑問 | 現場でのリアルな悩み・課題 |
| :--- | :--- | :--- |
| **Q1: チーム帰属** | **巨大なRailsモノリスなのに、どうやってコードと担当チームを全自動で紐付けているのか？** | 「物理的にマイクロサービスへ分割しないと、チーム別の監視やバジェット管理は無理なのではないか」 |
| **Q2: タグの徹底** | **開発者がタグ付けを忘れたりサボったりするのを、どうやって防いでいるのか？** | 「マニュアル遵守に頼ると必ず抜け漏れが発生し、メトリクスが不正確になるのではないか」 |
| **Q3: 定義の管理** | **数十チーム・数百エンドポイントのSLI/SLOを、どうやって破綻せずに一元管理しているのか？** | 「チームごとに監視設定がバラバラになり、組織全体での標準化が不可能になるのではないか」 |
| **Q4: 画面の自動化** | **Grafanaのダッシュボードをコードから自動生成する仕組みはどう動いているのか？** | 「手動でGrafanaをポチポチ作るとメンテナンス不能になるが、GitOpsでどう自動化しているのか」 |
| **Q5: 運用の自動化** | **バジェットが枯渇したチームを、どうやって人手をかけずに検知・フォローしているのか？** | 「SREが毎月ダッシュボードを監視して注意しに行くのは運用コストが高すぎるのではないか」 |
| **Q6: 障害復旧支援** | **アラートが鳴ったとき、不慣れな開発者がパニックにならずに対応できる仕組みはあるのか？** | 「アラートを見てもどこをどう調査すればいいか分からず、結局SREにエスカレーションされるのではないか」 |

---

## GitLabプラットフォームチームが提供する6大セルフサービス基盤

GitLabのプラットフォームチームが提供している、具体的な自動化アーキテクチャの詳細です。

---

### 第1章：コードレベルでの所有権宣言（Feature Category）

GitLabはRuby on Railsで書かれた巨大なモノリシックリポジトリ（GitLab Monolith）です。マイクロサービスに分割されていなくても、GitLabは**コードレベルでチーム（Stage Group）への自動帰属**を実現しています。

#### 1. エンドポイント・ジョブへの所有権宣言
開発者は、コントローラー、Sidekiqワーカー（非同期ジョブ）、GraphQLリゾルバ、APIエンドポイントのクラス定義に、所有する機能カテゴリ（`feature_category`）を1行宣言します。

```ruby
# Rails コントローラーでの宣言例
class Projects::MergeRequestsController < Projects::ApplicationController
  # このコントローラーの全アクションは「Code Reviewチーム」に帰属する
  feature_category :code_review_workflow

  def show
    # マージリクエスト表示処理
  end
end

# Sidekiq（非同期ジョブ）での宣言例
class PipelineProcessWorker
  include ApplicationWorker
  # このバックグラウンド処理のSLIは「Pipeline Executionチーム」に帰属する
  feature_category :continuous_integration

  def perform(pipeline_id)
    # パイプライン実行処理
  end
end
```

#### 2. Prometheusメトリクスへの自動ラベル伝播
ユーザーがリクエストを送信すると、Webサーバーのミドルウェアが自動的に `feature_category="code_review_workflow"` というラベルをHTTPアクセスログとPrometheusメトリクスに付与します。
開発者はログ出力コードやPrometheusクライアントを直接触る必要が一切ありません。

| 疑問へのアンサー |
| :--- |
| **【Q1への回答】巨大モノリスでも「コード1行のタグ付け」でチーム境界を自動集計**<br>インフラやリポジトリを物理的にマイクロサービスへ分割しなくても、コードレベルで `feature_category` を宣言することで、すべてのWebリクエストと非同期ジョブが担当チームのバジェットに全自動で帰属します。 |

---

### 第2章：CI静的解析によるタグ付け強制（Danger / RuboCop）

チーム別エラーバジェットを成立させるための最大の難所は、「開発者が新しいコードを書いたときにタグ付けを忘れる」ことです。
GitLabでは、**開発者の良心やマニュアル遵守に頼るのではなく、CIで機械的にガード** しています。

#### 1. CIパイプラインでの自動検査フロー
1. 開発者が新しいコントローラーや非同期ジョブ（Sidekiq）を作成し、マージリクエスト（MR）を起票する。
2. CIパイプライン上で **Danger（PR自動レビューBot）** および **RuboCopカスタムルール** が起動。
3. コード内に `feature_category :xxx` の宣言が存在しない場合、**CIが自動的にエラーを出してマージをブロック**。
4. 正しいカテゴリ名の一覧（全社共通マスター）と突合し、タイポ（スペルミス）も弾く。

これにより、本番環境にデプロイされるすべてのコードが例外なく担当チームに紐付けられます。

| 疑問へのアンサー |
| :--- |
| **【Q2への回答】CI静的解析による機械的ブロックで、タグ付け漏れを原理的に撲滅**<br>人間によるレビューに頼らず、CIが自動でマージを止めるガードレールを敷くことで、100%正確なメトリクス収集を担保します。 |

---

### 第3章：Metrics Catalog（YAML/JsonnetによるSLI/SLOの一元定義）

GitLabでは、監視設定を各サーバーやGrafanaのUIで直接編集することを固く禁止しています。
システム全体のSLI/SLO定義は、**`metrics-catalog` という単一のリポジトリでコード（Jsonnet / YAML）として一元管理** されています。

#### 1. 宣言的なSLI定義の例
開発者は、以下のような宣言的な定義ファイルを記述するだけで、複雑なPromQLを1行も書く必要がありません。

```jsonnet
// Jsonnet によるサービスSLI定義の例
{
  name: 'web-service',
  featureCategory: 'code_review_workflow',
  sli: {
    apdex: {
      targetDuration: 1.0, // 1秒以内を満足（Satisfied）と判定
    },
    errorRate: {
      threshold: 0.0005, // エラー率 0.05% 未満（目標可用性 99.95%）
    },
  },
}
```

#### 2. Prometheusルールへの全自動コンパイル
プラットフォーム基盤のCI/CDパイプラインがこのJsonnet定義を読み込み、Prometheusの記録ルール（Recording Rules）およびアラートルール（Alerting Rules）のYAMLを全自動で生成・デプロイします。

| 疑問へのアンサー |
| :--- |
| **【Q3への回答】Metrics Catalogによる宣言的定義で全社標準化と保守性を両立**<br>Jsonnetによる宣言的カタログを採用することで、開発者は複雑なPromQLを書くことなく、数行の定義追加だけで組織標準に準拠したSLI/SLOを安全に展開できます。 |

---

### 第4章：Dashboard as Code（Grafanaダッシュボードの完全自動生成）

GitLabでは、全ステージグループ（チーム）分のGrafanaダッシュボードを手動で作ることは一切ありません。
**Jsonnetライブラリ（Grafonnet）を用いて、ダッシュボードをコードから100%自動生成** しています。

#### 1. 自動生成されるチームダッシュボードの構成
コードにタグ（`feature_category`）が1つ追加されると、プラットフォーム基盤が以下のパネルを自動配置した専用ダッシュボードを生成します：
* **直近28日間のエラーバジェット残量ゲージ**（20.16分からの消費状況）
* **Apdex（速度）の推移グラフ**（目標1秒に対する達成率）
* **エラー率（500系エラーおよびSidekiq失敗）の推移グラフ**
* **バジェットを最も消費しているエンドポイント Top 5**（改善対象の特定）

#### 2. メリット：全ダッシュボードの一括アップグレード
デザイン変更や新しいメトリクスの追加が必要になった場合も、プラットフォームチームが共通テンプレート（Jsonnet）を1行更新してCI/CDを実行するだけで、数百枚の全チームダッシュボードが一括で最新化されます。

| 疑問へのアンサー |
| :--- |
| **【Q4への回答】GrafonnetによるDashboard as Codeで画面作成・保守コストを完全ゼロ化**<br>ダッシュボード生成パイプラインを構築することで、各チームの画面作成作業を撲滅し、常に全社で統一された高品質な可視化環境を提供します。 |

---

### 第5章：Triage Bot によるIssue自動起票とエスカレーション

バジェットが枯渇したチームのフォローに、SREやインフラエンジニアの人手を割く必要はありません。
GitLabでは **`gitlab-triage` Bot** がバジェットの監視と通知を自動化しています。

#### 1. 自動通知とIssue起票の流れ
* **日次・週次チェック**: BotがPrometheusの集計データを定期確認。
* **20分超過の検知**: 直近28日間の消費時間が20分を超えたチームを自動検知。
* **Issueの自動起票**: 該当チームのリポジトリに「エラーバジェット超過・改善計画」という定型Issueを自動起票。
* **担当者メンション**: チームのプロダクトマネージャー（PM）とエンジニアリングマネージャー（EM）に自動メンションし、次のスプリントでの工数50%改善を促す。

| 疑問へのアンサー |
| :--- |
| **【Q5への回答】Triage Botによる自動Issue起票でSREの運用・連絡コストを排除**<br>人手による監視や叱責をなくし、Botが機械的にIssueを起票してPM/EMに通知することで、感情的な対立を防ぎながら確実に改善プロセスを起動します。 |

---

### 第6章：Runbook as Code（アラートからの手順書自動連携）

開発チームがオンコール（障害一次対応）を担当する際、最も大きな障壁は「アラートを見てもどう対応すべきか分からない」という認知負荷です。
GitLabでは、すべてのアラートに **対応手順書（Runbook）のリンクが自動埋め込み** されています。

#### 1. Runbook連携の仕組み
* 各SLOアラートの定義内に、GitLab公式リポジトリ内のMarkdown手順書（`runbooks/` 配下）のURLを紐付け。
* Slack通知やGrafanaのアラート画面に **「Troubleshooting Runbook」** というボタンが直接表示される。
* 担当者はボタンを1クリックするだけで、
  - 「初動で確認すべきログの抽出クエリ」
  - 「過去の類似障害と緩和策（キャッシュクリア、フィーチャーフラグ無効化）」
  - 「担当チームへのエスカレーション先」
  を即座に確認できる。

| 疑問へのアンサー |
| :--- |
| **【Q6への回答】Runbook as Codeで一次対応手順をアラートに直結させ、MTTRを短縮**<br>アラートとドキュメントをコード上で1対1に紐付けることで、不慣れな開発者でも迷わず初動対応を行えるようにし、チームの復旧力（レジリエンス）を高めます。 |

---

## チームトポロジー視点：プラットフォームを成功させる3大原則

『チームトポロジー』の理論に基づき、プラットフォームエンジニアリングを成功させるための組織原則です。

### 1. Platform as a Product（プロダクトとしてのプラットフォーム）
* プラットフォームチームの「顧客」は社内の開発者（ストリーム専従チーム）です。
* 機能を一方的に押し付けるのではなく、**「開発者が自発的に使いたくなる、使いやすいインターフェース（セルフサービスAPI・CIツール）」** として設計・改善し続けます。

### 2. Thinnest Viable Platform（TVP: 最も薄い実行可能プラットフォーム）
* 最初から巨大で重厚な社内フレームワークを作ろうとしてはいけません。
* YAML数行のカタログや既存OSS（Prometheus/Grafana）を組み合わせた **「最小限で動く最も薄い仕組み」** から始め、必要に応じて段階的に拡張します。

### 3. イネーブリングチームによる自走化支援
* プラットフォームを導入する際、SREが「イネーブリングチーム（Enabling Team）」として開発チームに一時的に伴走（ファシリテーション）します。
* 監視の考え方や改善スキルを移転し、チームが自走できるようになったら立ち去ることで、プラットフォームへの依存を防ぎます。

---

## 自社で真似するための3ステップ導入ロードマップ

GitLabの大規模基盤を参考に、自社で小さく始めるための現実的なステップです。

| フェーズ | プラットフォームチームの取り組み | 開発チームの体験 |
| :--- | :--- | :--- |
| **ステップ1<br>【テンプレート化】** | 共通のGrafanaダッシュボードテンプレート（JSON）と、タグ付けルール（URL命名規約等）を策定・配布。 | 決められたURLルールに従ってAPIを作るだけで、基本ダッシュボードでエラー率が見える。 |
| **ステップ2<br>【CI/CD自動化】** | PR作成時のタグ検査（GitHub Actions / CIスクリプト）と、Grafana APIを使ったダッシュボード自動生成を導入。 | タグの付け忘れがCIで自動防止され、ダッシュボードが自動で更新される。 |
| **ステップ3<br>【Bot通知と完全自走】** | Slack BotやIssue起票スクリプトを導入し、バジェット超過時の通知とRunbook連携を自動化。 | バジェット超過時に自動でIssueが立ち、Runbookを見ながら自律的に改善スプリントを実施。 |

---

## 番外：心に留めておきたい「プラットフォーム設計の心得」

| 心得 | プラットフォームエンジニアへの示唆 |
| :--- | :--- |
| **① プラットフォームの成功基準は「開発者の認知負荷が下がったか」である** | 技術的に高度な基盤を作っても、開発者が使い方に悩むなら失敗。利用者の学習コストを極小化する。 |
| **② 開発者の日常ワークフロー（Git / CI）に溶け込ませる** | 独自の管理画面や複雑な社内ポータルを増やすのではなく、いつものPRレビューやCIチェックの中で完結させる。 |
| **③ 「取り締まる警察」ではなく「舗装された道路（Paved Path）」を作る** | ルールで縛るのではなく、「プラットフォームに乗るのが一番早くて楽だ」と開発者が自然に選ぶ環境を整える。 |

---

## まとめ

GitLabのエラーバジェット運用が世界最高峰の自律性とスピードを誇る真の理由は、**「プラットフォームチームが、開発者の認知負荷を徹底的にゼロにするセルフサービス基盤を作り上げているから」** です。

* **Feature Category と CI静的解析** により、コードのチーム帰属と抜け漏れ防止を完全自動化する。
* **Metrics Catalog と Dashboard as Code** により、監視の作成・保守コストを完全ゼロ化する。
* **Triage Bot と Runbook as Code** により、超過検知と一次対応を自動化する。
* **TVP思考** により、最小限の使いやすい仕組みで開発者のFast Flowを支える。

エラーバジェットやSLOを組織に定着させたいすべてのエンジニアリング組織にとって、このプラットフォームエンジニアリングの実践は最大の道標になります。

---

### 公式リファレンス・引用文献

1. **GitLab プラットフォーム基盤リポジトリ・ドキュメント（一次情報）**:
   * [GitLab Runbooks Repository (Metrics Catalog & Dashboards)](https://gitlab.com/gitlab-com/runbooks) —— GitLab本番監視基盤の全コード公開リポジトリ
   * [GitLab Metrics Catalog Documentation](https://gitlab.com/gitlab-com/runbooks/-/tree/master/metrics-catalog) —— SLI/SLOのJsonnet定義カタログ
   * [GitLab Dashboard as Code (Grafonnet)](https://gitlab.com/gitlab-com/runbooks/-/tree/master/dashboards) —— Grafana自動生成パイプライン
   * [GitLab Triage Operations](https://gitlab.com/gitlab-org/gitlab-triage) —— バジェット超過Issueの自動起票Bot
   * [GitLab Feature Categorization](https://docs.gitlab.com/ee/development/feature_categorization/) —— コードレベルでの所有権タグ付け規約
2. **組織・アーキテクチャ理論**:
   * Matthew Skelton, Manuel Pais 著『チームトポロジー — 価値あるソフトウェアを継続的に届けるプロポーショナルな組織設計』（日本能率協会マネジメントセンター）

---

### シリーズ記事一覧
* **第1部（運用・組織編）**: [【SRE×チームトポロジー】GitLabに学ぶ「チーム別エラーバジェット」——認知負荷を下げてFast Flowを実現する組織と監視の設計](file:///Users/taiga.ogura/git/study/blog/2026-08-29_gitlab-error-budgets.md)
* **第2部（基盤・実装編）**: 本記事
